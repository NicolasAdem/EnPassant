Priority order

Must-have for v1 to be honest about what it claims to do

[DONE] 1. Round-robin pairing mode. Circle-method schedule, byes for odd
   player counts, color balance within ±1 (even n) / ±2 (odd n). Integrates
   with the existing round-based model — zero schema changes. Verified with
   tests covering n=2 through n=16.

[NEXT] 2. Friend's exact Swiss algorithm. The current pair_swiss works for
   the easy cases but its rematch fallback is one swap. With many rounds in
   a tight group, it'll produce duplicate pairings or float people who
   shouldn't float. The pseudocode's swap-then-backtrack is the standard
   correct approach. Invisible until it breaks — but when it breaks, it
   breaks the tournament.

Should-have before showing this to a real club

[TODO] 3. End-to-end check: QR join + live ticker. Both are in the README
   as differentiators. The projector template has the QR overlay and ticker
   rendering; we haven't yet verified the player flow actually completes
   from a phone scan, or whether the ticker has anything to display. If
   either is half-built, it has to be finished before anything else — the
   whole pitch falls apart otherwise.

[TODO] 4. Buchholz + Sonneborn-Berger tiebreaks. Schema already has a
   buchholz column. pairing.py has a calculate_buchholz function. So
   Buchholz is half-done. Tournaments with ties at the top can't crown a
   winner without these. Quick to finish.

[DONE] 5. Recommended-mode hint on /new. Shipped alongside #1 — a player-
   count field on the create page now suggests Round-robin (≤12) or Swiss
   (>12) and auto-selects the radio if the host hasn't picked manually.

Nice-to-have, not blocking

[TODO] 6. Bye handling polish. Current code: if everyone has had a bye,
   lowest-score gets it. That's fine. But a player can technically get two
   byes in a small tournament if you cycle through. Worth tightening but
   not urgent.

[TODO] 7. On-site / off-site setting + table numbers. Useful for the chess-
   club case but cosmetic — the tournament works without it. Schema change
   (location_mode on tournaments, table_number on matches), UI changes on
   host create, host dashboard, projector, player view.

[TODO] 8. Round timer on projector. Mentioned in the original brief, not
   built. Genuinely adds drama to the projector view. Pure frontend.

Polish, do last

[TODO] 9. Multiple dashboard themes. CSS-only swap. Cool but adds zero
   functionality. The kind of thing that's fun to build and easy to ship
   after the engine is solid.

[TODO] 10. "Just happened" rank-change ticker copy. ("Alice climbed from #7
    to #3"). Currently the ticker just shows result messages. Adding rank
    deltas is one function in the result-recording flow.

[TODO] 11. Color balance refinement in Swiss. Current _assign_colors is
    reasonable but doesn't track "consecutive same color" — FIDE Swiss
    avoids three-in-a-row of one color. Edge case, school clubs won't
    notice.

[TODO] 12. Player-side dispute UI. The schema has disputed status. We
    haven't yet seen player.html to know whether the dispute flow exists.
    If not, this becomes critical, not nice-to-have — it's part of the
    USP.

---

Out-of-band fixes also done:

[DONE] tournament.py: start_next_round now returns an error dict when the
   pairing engine returns no pairings (which happens at the end of a
   round-robin schedule). Without this, the host's "Start round" button
   would silently create an empty round and bump current_round forever.

[DONE] pairing.py: type-annotation cleanup. Pylance was warning about
   nullable types in three places (paired_so_far holds Optional partners
   for emergency byes; ids in _circle_schedule holds an Optional ghost).
   No behaviour change — just narrower types widened to match what the
   code actually does. Also dropped an unused parameter on _pair_group.

Verified vs. the original pair_swiss (A/B comparison, "white always wins"
stress test):
   - Old: produced duplicate pairings (rematches) under stress — 39 dupes
     in a 12-round 16-player simulation.
   - New: zero duplicate pairings across all tested configs. Instead, when
     no legal pairing exists for a player, an emergency bye is awarded —
     a visible signal to the host instead of a silent rematch.